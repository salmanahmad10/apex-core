# apex-core

Generated by Nudgd

## Project Structure

This is a full-stack application with the following structure:

```
apex-core/
├── server/           # Node.js backend (Express + TypeScript)
└── web/              # Next.js 14 frontend
```

## Getting Started

### 🐳 Database Setup (Fastest Way)

No need to install PostgreSQL locally. Just run:

```bash
docker-compose up -d
```

This will start a PostgreSQL 15 container. Then run:

```bash
npx prisma db push
```

### Backend Setup

1. Navigate to the server folder:
```bash
cd server
```

2. Install dependencies:
```bash
npm install
```

3. Configure environment variables:
```bash
# Copy .env.example to .env and update values if needed
cp .env.example .env
```

4. Push the database schema:
```bash
npx prisma db push
```

5. Start the development server:
```bash
npm run dev
```

The backend will run on `http://localhost:4000`

### Frontend Setup

1. Navigate to the web folder:
```bash
cd web
```

2. Install dependencies:
```bash
npm install
```

3. Start the development server:
```bash
npm run dev
```

The frontend will run on `http://localhost:3000`

## Tech Stack

### Backend
- Node.js + Express
- TypeScript
- PostgreSQL + Prisma
- JWT Authentication
- stripe Payments
- resend Email Service
- openai AI Integration
- s3 File Storage

### Frontend
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Axios

## Documentation

See individual README files in `server/` and `web/` folders for more details.

## License

MIT
